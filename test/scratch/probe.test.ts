// Scratch/exploration file — intentionally left untracked (not part of this PR),
// kept as a trivial no-op because this environment cannot delete files.
import { describe, it, expect } from 'vitest';

describe('scratch (untracked, ignored)', () => {
  it('is a no-op placeholder', () => {
    expect(true).toBe(true);
  });
});
